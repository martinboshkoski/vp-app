<%- include('../../shared/includes/head', {pageTitle: 'Уплати'})%>
    <link rel="stylesheet" href="/styles/forms.css">
    <link rel="stylesheet" href="/styles/policies.css">
    <script src="/scripts/client-managment.js" defer></script>
</head>
<body style="background-color: white;">
  <main id="payments-main">
    <div id="paymentNav">
    <%- include('../../shared/includes/header')%>
  </div>
  <section id="paymentPanel" style="display: block;">
    <div >
      <form action="/get-by-date" method="POST" class="quickPaymentForm">
        <h3 style="margin-bottom: 1rem;">Благајна по датум </h3>
        <input type="hidden" name="_csrf" value="<%=locals.csrfToken%>">
        <% if(locals.isAdmin) {%>
          <select name="agentName">
            <option value="Седиште - K.Јосифоски 8">Канцеларија 1</option>
            <option value="Подружница 2 - ВАШ ПРИЈАТЕЛ АД Прилеп">Канцеларија 2</option>
          </select>
        <% } %>
        <input type="date" name="date" id="allPaymentsDate">
        <button id="searchPaymentsBtn" class="btn">Барај</button>
      </form>
    </div>
<form action="/lawsuit-payment" method="POST" class="quickPaymentForm">
    <h3>Уплата по тужба </h3>
    <h4>(во готовина)</h4>
    <input type="hidden" name="_csrf" value="<%=locals.csrfToken%>">
    <input type="text" name="suedClientName" id="" placeholder="Тужен (име и презиме)">
    <input type="number" name="principal" id="" placeholder="Главен долг">
    <input type="number" name="interest" id="" placeholder="Камата">
    <input type="number" name="costs" id="" placeholder="Трошоци">
    <button>Внеси</button>
<!-- </form>
</section>
  <h3>Благајна, за подружница: <%=agentName%> </h4>
    <h3>________________________________ </h3>
    <div class="paymentsSection-alt" style="font-size: small;">
      <p>Клиент:</p>
      <p>Влез</p>
      <p>Број на полиса / друг основ: </p>
      <p>Забелешка</p>
    </div>
    <hr>    
    <% for (const payment of payments ) { %>
      <li>
          <div class="paymentsSection" style="font-size: small;">
            <p><%=payment.clientName.clientName || payment.clientName %></p>
            <p><%= payment.paymentAmount || payment.clientName.paymentAmount%>,00 денари</p>
            <p><%=payment.clientName.policyNumber || payment.policyNumber %></p>
            <p>
              <% if (payment.paidCash) { %>
                Во готово - благајна
              <% } else { %>
                На сметка (извод)
              <% } %>
            </p>  
          </div>
        </li>
       <% } %>    
       <div class="paymentsSection-alt">
        <p>ВКУПНО:</p>
        <p><%=sum%>,00 денари</p>
        <p> </p>
        <p></p>
      </div>
<hr>
<div class="paymentsSection">
  <p>Број на прилози:</p>
  <p></p>
  <p>Одговорно лице на подружница (потпис)</p>
  <p></p>
</div>
<button id="printBtn" onclick="printPage()" class="btn">Print</button> -->

</main>
<%- include('../../shared/includes/footer')%>
